<!DOCTYPE html>
<html lang="{{@site.locale}}">
<head>
  {{!-- Document Settings --}}
  <meta charset="utf-8">

  {{!-- Base Meta --}}
  <title>{{meta_title}}</title>
  <meta name="robots" content="index, follow">
  <meta name="HandheldFriendly" content="True">
  <meta
    name="viewport"
    content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"
  >
  <meta name="format-detection" content="telephone=no">
  <meta name="format-detection" content="email=no">

  <link rel="stylesheet" href="{{asset 'libs/prism/prism.css'}}">
  <link rel="stylesheet" href="{{asset 'css/variables.css'}}">
  <link rel="stylesheet" href="{{asset 'css/basic.css'}}">
  <link rel="stylesheet" href="{{asset 'css/global.css'}}">
  <link rel="stylesheet" href="{{asset 'css/main.css'}}">
  <link rel="stylesheet" href="{{asset 'css/widgets.css'}}">
  <link rel="stylesheet" href="{{asset 'css/mobile.css'}}">

  {{!-- This tag outputs SEO meta, structured data and other important settings --}}
  {{ghost_head}}
</head>
<body
  class="{{body_class}} theme-turquoise"
  {{#if @site.cover_image}}style="background-image: url({{@site.cover_image}})"{{/if}}
>
  <div id="main-wrapper" class="clearfix">
    <main id="main" class="fl" role="main">
      {{!-- All the main content gets inserted here, index.hbs, post.hbs, etc --}}
      {{{body}}}

      {{!-- The footer at the very bottom of the screen --}}
      {{> 'footer'}}
    </main>

    <aside id="sidebar" class="fl">
      {{> 'widget-blog-nav'}}
      {{> 'widget-featured-posts'}}
      {{> 'widget-tags-cloud'}}
      {{> 'widget-blog-stat'}}
      {{> 'widget-blog-follow'}}
    </aside>
  </div>

  {{> 'mobile-header'}}
  {{> 'mobile-nav'}}

  {{!-- Scripts --}}
  {{{block 'prism-scripts'}}}
  <script src="{{asset 'js/main.js'}}"></script>

  {{!-- Ghost outputs important scripts and data with this tag - it should always be the very last thing before the closing body tag --}}
  {{ghost_foot}}
</body>
</html>